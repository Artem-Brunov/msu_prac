
var text;
var synth;
var ttsObject;
var window.list = add();

function add()
{	
	window.list += document.getElementById("TTS").value;
	alert(window.list);
}

function talk() 
{

	synth = window.speechSynthesis;
	ttsObject = new SpeechSynthesisUtterance(voices);
	
	synth.speak(ttsObject);
}

function stop()
{
	var pp = add();
	alert(pp);
	synth.pause();
}