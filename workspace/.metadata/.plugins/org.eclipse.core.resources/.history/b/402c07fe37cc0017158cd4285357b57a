var voices = [];
var text;
var synth;
var ttsObject;

function add()
{
	window.voices.push(document.getElementById("TTS").value);

}
function talk() 
{
	
	synth = window.speechSynthesis;
	ttsObject = new SpeechSynthesisUtterance(voices);
	
	synth.speak(ttsObject);
}

function stop()
{
	alert("LOL");
	synth.pause();
}