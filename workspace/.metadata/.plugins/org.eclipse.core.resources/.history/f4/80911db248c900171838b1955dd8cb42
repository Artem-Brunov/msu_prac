var voices = [];
var text;
var synth;
var ttsObject;


function add()
{
	alert(voices.length);
	voices = voices.push(document.getElementById("TTS").value);
	return voices;
}
function talk() 
{
	
	synth = window.speechSynthesis;
	ttsObject = new SpeechSynthesisUtterance(voices);
	
	synth.speak(ttsObject);
}

function stop()
{
	synth.pause();
}