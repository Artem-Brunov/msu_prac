var voices;
var text;
var synth;
var ttsObject;

alert(voices);
function add(m)
{
	window.voices.push(m);

}
function talk() 
{
	
	synth = window.speechSynthesis;
	ttsObject = new SpeechSynthesisUtterance(voices);
	
	synth.speak(ttsObject);
}

function stop()
{
	synth.pause();
}